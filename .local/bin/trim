#! /usr/bin/env python3

"""Simple utility to trim whitespace from a string.

The string to be trimmed should be piped into stdin.
"""

import sys

sys.stdout.write(sys.stdin.read().strip())
sys.stdout.write("\n")
